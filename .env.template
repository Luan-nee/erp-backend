# .env.template
#
# Copia este fichero a `.env` y rellena los valores reales antes de ejecutar
# NO subas tus credenciales reales a un repositorio público.
#
# MySQL / Base de datos
# - DB_HOST: dirección del servidor MySQL (localhost o una IP/hostname)
# - DB_USER: usuario de la base de datos
# - DB_PASSWORD: contraseña del usuario de la base de datos
# - DB_NAME: nombre de la base de datos a usar/crear
# - DB_PORT: puerto TCP del servidor MySQL (por defecto 3306)
#
# Ejemplo de uso local:
# DB_HOST=localhost
# DB_USER=root
# DB_PASSWORD=tu_contraseña_segura
# DB_NAME=erp_app
# DB_PORT=3306

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=erp_app
DB_PORT=3306

BACKEND_PORT=3000

# Opcionales / recomendaciones
# - NODE_ENV: entorno de ejecución ('development' | 'production')
# - PORT: puerto en el que se escucha el servidor Express (por defecto 3000)
# - DB_CONN_LIMIT: límite de conexiones del pool (opcional para tuning)
#
# Ejemplo adicional:
# NODE_ENV=development
# PORT=3000
# DB_CONN_LIMIT=10